const Esports = ({ isLoading, items }) => {
  //   return isLoading ? (
  //     <h1 style={{ color: 'white' }}>Loading...</h1>
  //   ) : (
  //     <section>
  //       {items.map((item) => (
  //         <h1>{item}</h1>
  //       ))}
  //     </section>
  //   );

  return (
    <div>
      <h1 style={{ color: 'white' }}>Loading...</h1>
    </div>
  );
};

export default Esports;
